# Proxy server for <a href="https://github.com/jballmann/safelink-outlook">Safelink Extension</a>
To run the server,
1. clone repo,
2. install dependencies with ```npm install```,
3. start server with ```node server.js```
